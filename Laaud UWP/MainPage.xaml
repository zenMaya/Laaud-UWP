<Page
    x:Class="Laaud_UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Laaud_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox x:Name="SearchTextBox" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Height="48" Background="#66FF00FF" PlaceholderText="Search" FontSize="24" TextChanged="SearchTextBox_TextChanged"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="19*"/>
                <ColumnDefinition Width="109*"/>
            </Grid.ColumnDefinitions>
            <ScrollViewer Margin="0,48,0,166" Background="#FFE8FF00" VerticalScrollBarVisibility="Hidden">
                <StackPanel x:Name = "TrackList" RelativePanel.AlignTopWithPanel="True" Background="#FFD1FF00">
                    <local:TracklistItem>

                    </local:TracklistItem>
                </StackPanel>
            </ScrollViewer>
            <Grid Margin="0,0,0,102" Background="#FFFFFF34" Height="64" VerticalAlignment="Bottom"/>

            <Grid Margin="0,48,0,104" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="11*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>
                <ScrollViewer VerticalScrollBarVisibility="Hidden" Grid.RowSpan="2" Margin="42,0,0,0">
                    <ListView x:Name="SearchResults" Background="Blue">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" 
                                        Margin="20,0,20,8"
                                        FontSize="24" 
                                        FontStyle="Italic" 
                                        FontWeight="SemiBold"
                                        Foreground="DarkBlue" /> 
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
                <ScrollViewer Grid.Column="1" Grid.RowSpan="2" VerticalScrollBarVisibility="Hidden">
                    <Grid Background="Green" CanDrag="True"></Grid>
                </ScrollViewer>
                <StackPanel HorizontalAlignment="Left" Width="42" Background="#FF00F3FF" Grid.RowSpan="2">
                    <Button x:Name="Settings" Height="42" HorizontalAlignment="Stretch" >
                        <Image Source="Assets/Settings.png" Margin="-11,-1"/>
                    </Button>
                    <Button x:Name="Favorites" Height="42" HorizontalAlignment="Stretch">
                        <Image Source="Assets/Favorites.png" Margin="-5,-7"/>
                    </Button>
                    <Button x:Name="Folders" Height="42" HorizontalAlignment="Stretch" />
                    <Button x:Name="Playlists" Height="42" HorizontalAlignment="Stretch" />
                    <Button x:Name="About" Height="42" HorizontalAlignment="Stretch"/>
                    <Button x:Name="AddMusicToLibrary" Height="42" HorizontalAlignment="Stretch" Click="AddMusicToLibrary_Click">
                        <Image Source="Assets/AddMusicToLibrary.png" Margin="-11,-1"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
        <Grid Background="Red" Height="104" VerticalAlignment="Bottom"/>
    </Grid>
</Page>